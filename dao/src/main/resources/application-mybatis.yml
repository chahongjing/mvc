spring:
  datasource:
    master:
      driver-class-name: org.sqlite.JDBC
      password:
      jdbcUrl: jdbc:sqlite::resource:db/db.db3
      username:
    slave:
      driver-class-name: org.sqlite.JDBC
      password:
      jdbcUrl: jdbc:sqlite::resource:db/db.db3
      username:
    migration:
      driver-class-name: org.sqlite.JDBC
      password:
      jdbcUrl: jdbc:sqlite::resource:db/db.db3
      username:

#mybatis:
#  configuration:
#    map-underscore-to-camel-case: true
#    lazy-loading-enabled: false
#    interceptors: com.zjy.dao.common.sql.SqlPrint

#mybatis-plus:
#  configuration:
#    map-underscore-to-camel-case: false
##    interceptors: com.zjy.dao.common.sql.SqlPrint
#    lazy-loading-enabled: false

pagehelper:
  helperDialect: sqlite