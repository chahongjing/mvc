spring:
  profiles:
    include: mybatis, common, @profiles.active@
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  mvc:
    static-path-pattern: /static/**
#  redis:
#    host: 127.0.0.1

server:
  error:
    include-exception: true
    include-message: always
  port: 8088

ttkey:
  ab: @mykey@